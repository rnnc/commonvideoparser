{
  "name": "commonvideoparser",
  "version": "1.0.0",
  "description": "Gets video info from popular video sites (Youtube, Vimeo, Dailymotion, Google Drive)",
  "main": "index.js",
  "engines": {
    "node": ">=8"
  },
  "scripts": {
    "start": "cross-env NODE_ENV=production node playground",
    "clean": "rimraf dist/* dist dev/* dev",
    "build:dev": "npm run clean && cross-env BABEL_ENV=development babel lib -d dev -s",
    "build:prod": "npm run clean && cross-env BABEL_ENV=production babel lib -d dist",
    "test": "jest"
  },
  "author": "Rohan C",
  "license": "MIT",
  "dependencies": {
    "moment": "^2.22.1",
    "request": "^2.85.0",
    "request-promise": "^4.2.2",
    "vimeo": "^2.0.2"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-jest": "^22.4.3",
    "babel-loader": "^7.1.4",
    "babel-plugin-transform-runtime": "^6.23.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-minify": "^0.4.0",
    "cross-env": "^5.1.4",
    "jest": "^22.4.3",
    "rimraf": "^2.6.2"
  }
}
